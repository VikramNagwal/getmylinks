{
  "dependencies": {},
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "lint": "cd client && bun run lint && cd .. && cd server && bun run lint",
    "format": "cd client && bun format && cd .. && cd server && bun format"
  }
}
